# DenseK3-Core Titan compatibility environment.
#
# PyTorch and Triton are installed separately from the NJU CUDA 12.4 mirror:
#   torch==2.5.1+cu124
#   triton==3.1.0 (required by that torch wheel)
#
# Keep this file independent from the canonical 5090 environment.  The Titan
# host has glibc 2.27, so the canonical torch 2.11 manylinux_2_28 wheel cannot
# be loaded safely.
accelerate==1.14.0
bitsandbytes==0.49.2
# Datasets 5 requires PyArrow >=21, whose Linux wheel requires glibc >=2.28.
# The newest binary-only pair compatible with Ubuntu 18.04/glibc 2.27 is used.
datasets==4.0.0
evaluate==0.4.6
einops==0.8.2
hf-xet==1.5.1
huggingface-hub==1.21.0
numpy==2.2.6
pandas==2.3.3
peft==0.19.1
pyarrow==20.0.0
pytest==9.1.1
pytest-xdist==3.8.0
PyYAML==6.0.3
ruff==0.16.2
safetensors==0.8.0
tensorboard==2.21.0
transformers==5.12.1
